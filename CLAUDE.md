# Chloe Ting Clone → Erijane

Follow `AGENTS.md` and `context/` (reading order in AGENTS.md).

## Stack
- Laravel 13, Livewire 4, Tailwind 4, PHP 8.3 + Filament admin

## Commands
```bash
composer install
npm install && npm run build
php artisan serve
php artisan test --filter=RouteSmokeTest
```

## AI Team Configuration
Routing: `Obsidian Vault/minf-think/agent-map.md` + `/minf-pipeline`.
Implement: laravel-backend-expert patterns; Livewire SFC under `resources/views/components/`.
Review: RouteSmokeTest + browser side-by-side vs https://chloeting.com/
