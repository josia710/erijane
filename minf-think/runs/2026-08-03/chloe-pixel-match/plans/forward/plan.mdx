---
title: Chloe Ting clone — pixel-match forward plan
kind: plan
confidence: pending-interview
---

# Forward plan — chloe pixel-match

## Goal
Match https://chloeting.com/ **structure, format, scroll/carousel behavior** on the Erijane Laravel clone without regressing brand/disclaimers.

## Confidence gate
Blocked at interview until T1–T3 answered (scope, carousel lib, token truth). Target ≥9 after answers.

## Selected stack
- **Rules:** Karpathy surgical; project CLAUDE.md; keep Erijane
- **Skills:** skillui (reference), DESIGN.md truth, impeccable critique/polish after structure, taste-skill dials, grilling-frontend-prototyping if motion look foggy
- **Execute seats (Stage C):** laravel-backend-expert, laravel-simplifier, frontend via Composer/Sonnet — dual-orchestrator lanes
- **Review:** code-reviewer (thermo-nuclear unavailable as Task type), RouteSmokeTest, side-by-side browser
- **Skip:** gpt-taste greenfield Awwwards rewrite; inventing GSAP if live is slick; re-applying skillui Poppins

## Gate B prerequisite (from council-advisor)
Amend `DESIGN.md` §1 MOTION dial + §6 before any carousel/scroll code — current text forbids the goal. Draft lives in `03-council-merge.md`.

## Execution sequence (draft — refine after T1–T4)
1. **cursor** — Apply approved DESIGN.md §6 amendment
2. **cursor** — CDP: slick settings + peek + desktop merch branch (U1)
3. **cursor** — Optional prototype Embla vs slick-vanilla
4. **cursor/sonnet** — Wire chosen engine in `app.js` + home merch; wire `hero_bg`
5. **cursor/sonnet** — Home structure/spacing polish; Manrope unless T3 says Poppins hero
6. **sonnet** — `npm run build` + `php artisan test --filter=RouteSmokeTest`
7. **fable/review** — laravel-simplifier + code-reviewer; ship-quiz

## Dual-engine lanes
| Step | Lane | Loop ring |
|------|------|-----------|
| Plan / boards | fable | oversight |
| Carousel + Blade | cursor or sonnet | execution |
| Tests/build | sonnet | task |
| Review | fable + code-reviewer | oversight |

## Loop (Stage C)
4-condition likely **fail** (not weekly cadence) → **manual Phase 4 once**, not `/loop`. Tag `--no-loop` default.

## Done-when
- [ ] Home section order matches live (documented checklist)
- [ ] Merch (and other home carousels) use real carousel behavior parity (autoplay/dots/drag as live)
- [ ] Erijane brand + footer disclaimer intact
- [ ] `php artisan test --filter=RouteSmokeTest` = 0; `npm run build` clean
- [ ] Side-by-side desktop+mobile recorded in STATE evidence

## Out of scope unless approved
- Full Next.js rewrite
- Auth/payments redesign
- Literal Ant Design dependency in Laravel (prefer Embla/Swiper parity)
- Listing-page deep parity beyond home (until T1 says otherwise)

## Analysis board
http://127.0.0.1:8766/
